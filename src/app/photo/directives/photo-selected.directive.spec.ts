import { PhotoSelectedDirective } from './photo-selected.directive';

describe('PhotoSelectedDirective', () => {
  it('should create an instance', () => {
    const directive = new PhotoSelectedDirective();
    expect(directive).toBeTruthy();
  });
});
